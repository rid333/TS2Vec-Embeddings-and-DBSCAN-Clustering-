### TS2Vec and DBSCAN Clustering

Density based clusetering on embedded data. Data used in this project consists of 24-hour monitoring results from flex sensors and heart rate sensors.

---

#### Setup and run
```bash
Install deps:
pip install -r req.txt

Run full pipeline:
python main.py
```

Example results on `plot/` folder
